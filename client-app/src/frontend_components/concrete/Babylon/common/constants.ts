import * as BABYLON from "@babylonjs/core";

export const ARC_ROTATE_CAMERA_ALPHA = Math.PI / 2;
export const ARC_ROTATE_CAMERA_BETA = Math.PI / 2;
export const ARC_ROTATE_CAMERA_RADIUS = 2;
export const ARC_ROTATE_CAMERA_TARGET = BABYLON.Vector3.Zero();

export const NO_RECURSE_DISPOSE_CAMERA = false;
export const DISPOSE_CAMERA_MAT_AND_TEXTURES = true;

export const WALL_HEIGHT = 3.0;
export const WALL_THICKNESS = 0.4;